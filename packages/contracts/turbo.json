{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "inputs": ["wagmi.config.ts", "abi/**/*.json"],
      "outputs": ["contracts.ts"]
    },
    "check": {
      "inputs": ["tsconfig.json", "wagmi.config.ts"]
    },
    "lint": {
      "inputs": ["wagmi.config.ts"]
    }
  }
}
